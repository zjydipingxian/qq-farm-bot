syntax = "proto3";

package gamepb.skinpb;

// ============ 皮肤信息 ============
message SkinItem {
    int64 skin_id = 1;
    int64 slot_type = 2;    // 装备槽位类型
}

// ============ 请求/回复 ============

// --- 获取已拥有皮肤 ---
message SkinsOwnedRequest {}

message SkinsOwnedReply {
    repeated SkinItem skins = 1;
}

// --- 获取已装备皮肤 ---
message SkinsEquippedRequest {}

message SkinsEquippedReply {
    repeated SkinItem skins = 1;
}
